apiVersion: v1
kind: Service
metadata:
  name: flask-todo-service
  namespace: flask-todo
spec:
  type: LoadBalancer
  selector:
    app: flask-todo
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
