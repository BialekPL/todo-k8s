apiVersion: v1
kind: Service
metadata:
  name: flask-todo-service-prod
  namespace: flask-todo
spec:
  type: LoadBalancer
  selector:
    app: flask-todo-prod
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
