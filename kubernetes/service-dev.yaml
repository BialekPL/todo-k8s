apiVersion: v1
kind: Service
metadata:
  name: flask-todo-service-dev
  namespace: flask-todo
spec:
  type: LoadBalancer
  selector:
    app: flask-todo-dev
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
